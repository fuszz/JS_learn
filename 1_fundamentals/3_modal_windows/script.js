'use strict';

// Displays simple box with info
// Returns nothing
alert("Hello from the alert box!");

// Input box
// Returns content of input field or null, if cancel pressed
let result = prompt("Some fancy title", ["Optional parameter: some default data"]);

// Confirm box
// Returns true or false
let areYouMad = confirm("Are you mad?");
